#include <stdio.h>

int main() {
       int n=11;
       if(n%5==0&&n%11==0)
       {
           printf("its divisible");
       }
       else
       {
           printf("not divisible");
       }
        return 0;
}